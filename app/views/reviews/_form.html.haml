.form
	.inner
		=simple_form_for([@artwork, @artwork.reviews.build], remote: true)do |f|
			.formItem.formInput.reviewInput
				= f.text_area :content
			.formItem
				= f.submit 'Submit Review', :class => "btn btn-m btn-light btn-submit"
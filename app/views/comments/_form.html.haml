.form
	.inner
		= form_for [@commentable, @comment] do |f|
			.formItem
				%h2 Reply to Comment
			.formItem
				= f.hidden_field :parent_id
			.formItem
				= f.text_area :comment
			.formItem
				= f.submit
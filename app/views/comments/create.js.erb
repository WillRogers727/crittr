$('.commentReplyForm').remove(); //remove forms
$('.activeReply').show(); //show link again
$('.replyLink.activeReply').removeClass('activeReply'); //remove active reply class from all of them
$('.activeCommentReplySection').append('<%= j render "comment", :comment => @comment %>');
$('.activeCommentReplySection').removeClass('activeCommentReplySection'); //remove active reply section
$('.commentInput textarea').val('');
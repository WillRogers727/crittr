%h3 Comments
/commentable variable is passed through and changes which comments are shown
/ - search_form_for @searchComments,remote: true do |f|
/ 	= sort_link @searchComments, :created_at, "Date Commented"


- commentable.comments.each do |comment| 
	#comment
		= comment.comment
		= comment.user.email
		%p
			Ups:
			= comment.get_upvotes.size
		%p 
			Downs:
			= comment.get_downvotes.size

		- if user_signed_in?
			%div.upArrow
				= link_to "Upvote", like_comment_path(comment), method: :put
				= comment.get_upvotes.size
			%div.downArrow
				= link_to "Downvote", dislike_comment_path(comment), method: :put
				= comment.get_downvotes.size

			- if current_user.id == comment.user_id
				=link_to "Delete Comment", [commentable, comment], method: :delete, data: {confirm: "Are you sure you want to do this?"}


%h3 Reply to thread